const sharedConfig = require('<%- sharedConfig %>')

/**
 * @type {import('prettier').Config}
 */
const config = {
  ...sharedConfig,

  overrides: [
    {
      files: ['*.html'],
      options: {
        singleAttributePerLine: false,
      },
    },
    {
      files: ['*.{css,sass,scss,less,postcss}'],
      options: {
        singleQuote: false,
      },
    },
  ],
}

module.exports = config
